{
  "name": "@ec-url-store/api",
  "version": "0.0.0",
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "dev": "cargo run",
    "start": "cargo run --release"
  }
}
